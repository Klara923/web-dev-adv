// build docker image
// docker build -t my-node-app .

// run the containers
// docker-compose up

//app runs on port:13000 and the public directory can be
//changed witout rerunning the docker containers
